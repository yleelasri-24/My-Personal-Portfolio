**Level:** Beginner

**Concepts Tested:**

HTML structure, text tags, links, images, semantic HTML, basic CSS, box model

### 📌 Task

Create a **one-page personal portfolio website** using only **HTML + CSS**.

### 📁 Folder Structure

```
portfolio/
├── index.html
└── style.css

```

### 📄 Requirements

### HTML

- Use **semantic tags**:
    - `<header>` → name + short intro
    - `<nav>` → links (Home, About, Projects, Contact)
    - `<section>` → About Me
    - `<section>` → Projects (at least 2 projects)
    - `<footer>` → copyright text
- Use:
    - Headings (`h1`–`h3`)
    - Paragraphs
    - Lists (for skills or hobbies)
    - At least **one image**
    - At least **one external link**

### CSS

- Use **external CSS only**
- Apply:
    - Colors (background + text)
    - Margin & padding
    - Border around sections or cards
    - Width & height where appropriate
    - Center content using **margin or text-align**
    - Style navigation links (hover optional but not required)

 ### 🚫 Restrictions

- ❌ No JavaScript
- ❌ No frameworks
- ❌ No Flexbox/Grid 
